import React from 'react';
import { Container } from 'react-bootstrap';

const Portfolio = () =>{
	return(
		<Container className="mt-5" style={{height: '100vh'}}>	
			<h1>Building Portfolio Page...</h1>
		</Container>
	);
}

export default Portfolio;