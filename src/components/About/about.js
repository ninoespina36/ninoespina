import React from 'react';
import { Container } from 'react-bootstrap';

const About = () =>{
	return(
		<Container className="mt-5" style={{height: '100vh'}}>	
			<h1>Building About Page...</h1>
		</Container>
	);
}

export default About;