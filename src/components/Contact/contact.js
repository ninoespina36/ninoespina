import React from 'react';
import { Container } from 'react-bootstrap';

const Contact = () =>{
	return(
		<Container className="mt-5" style={{height: '100vh'}}>	
			<h1>Building Contact Page...</h1>
		</Container>
	);
}

export default Contact;