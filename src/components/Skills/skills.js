import React from 'react';
import { Container } from 'react-bootstrap';

const Skills = () =>{
	return(
		<Container className="mt-5" style={{height: '100vh'}}>	
			<h1>Building Skills Page...</h1>
		</Container>
	);
}

export default Skills;