import React from 'react';
import { Container } from 'react-bootstrap';

const Resume = () =>{
	return(
		<Container className="mt-5" style={{height: '100vh'}}>	
			<h1>Building Resume Page...</h1>
		</Container>
	);
}

export default Resume;